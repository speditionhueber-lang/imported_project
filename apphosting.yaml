# Firebase App Hosting configuration.
# Optional integration keys are configured in the Firebase console when available.
# They are intentionally not required for building and deploying the core app.

runConfig:
  maxInstances: 1
